{
  "name": "@first2apply/node-backend",
  "version": "0.0.0",
  "description": "",
  "main": "build/main.js",
  "typings": "build/main.d.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "npx nodemon --exec \"node -r ts-node/register/transpile-only -r dotenv/config --inspect=0.0.0.0:9229 src/main.ts\"",
    "build": "tsc -p tsconfig.json"
  },
  "keywords": [],
  "author": "BeastX Industries",
  "license": "MIT",
  "packageManager": "pnpm@10.19.0",
  "dependencies": {
    "@first2apply/core": "workspace:*"
  }
}
